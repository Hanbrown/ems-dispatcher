import React from "react";

import Button from "react-bootstrap/Button";

const AddEntry = () => {



	return(
			<Button variant="info" onClick={this.props.click}>Add Object</Button>
	);
};

export default AddEntry;